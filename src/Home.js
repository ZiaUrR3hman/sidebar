import React, { useContext } from 'react';
import { FaBars } from 'react-icons/fa';

const Home = () => {
	return (
		<main>
			<button className="tex">
				<FaBars />
			</button>
		</main>
	);
};

export default Home;
